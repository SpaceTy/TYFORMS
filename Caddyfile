apply.spacety.dev {
    # Reverse proxy to your Go application
    reverse_proxy /api/* localhost:5099

    # Serve static files and handle SPA routing
    root * wwwroot
    file_server
    try_files {path} /index.html

    # Enable automatic HTTPS
    tls {
        protocols tls1.2 tls1.3
    }
}